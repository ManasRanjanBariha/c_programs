// find the area of tringle
#include <stdio.h>
int main(int argc, char const *argv[])
{
    int area,len,bre;
    printf("Enter the length and bredth  of the tringle ");
    scanf("%d%d",&len,&bre);
    printf("the area of the rectangle is %d",len*bre);
    return 0;
}